name: .NET Core

on:
  push:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-18.04
    env:
      CI: ""

    steps:
    - uses: actions/checkout@v2

    - uses: Azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS}}

    - uses: Azure/get-Keyvault-secrets@v1.0
      with: 
        keyvault: "TailwindTraders-Demo-KV"
        secrets: 'DockerPassword'
      id: kvSecretAction

    - uses: Azure/docker-login@v1
      with:
        login-server: ttacrbyskvloe5jfqa.azurecr.io
        username: ttacrbyskvloe5jfqa
        password: ${{ secrets.DOCKER_PASSWORD }}

    - run: |
        cd TailwindTraders.Website/Source/Tailwind.Traders.Web
        docker build -t ttacrbyskvloe5jfqa.azurecr.io/tailwindtraders-ado40:${{ github.sha }} -t ttacrbyskvloe5jfqa.azurecr.io/tailwindtraders-ado40:latest .
        docker push ttacrbyskvloe5jfqa.azurecr.io/tailwindtraders-ado40:latest
        docker push ttacrbyskvloe5jfqa.azurecr.io/tailwindtraders-ado40:${{ github.sha }}
        cd ..